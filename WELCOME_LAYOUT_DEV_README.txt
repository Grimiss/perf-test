D8M4 CP06 Welcome Layout Dev RC3

- RC20/RC19 navigation behaviour retained.
- Welcome background replaced with the approved rendered CRT artwork crop.
- Development panel remains draggable by its title bar.
- Layout editor no longer uses a MutationObserver on the live CRT. This fixes the browser hang/crash caused by frequent CRT state updates.
- Layout autosave keys were bumped to V3/V2 so older dev positions do not interfere.
- Use EDIT ON, select a target, drag/resize, then EXPORT JSON when approved.

RC5: Restored rendered Welcome artwork. STATUS removed. PRESS PLAY text removed (button retained). Typed guidance remains selectable. Up/Down keyboard navigation added as a standing CRT menu rule; Enter activates focused menu items. Header typo corrected in code overlay: DRONE MACHINE [D8M4], with red model number and yellow instruction line.
